#clone
git clone https://github.com/quanted/qed_splash.git

#after cloning a project with submodules
git submodule update --init --recursive #fetch data and checkout appropriate project

#update splash_app
cd splash_app
#head will be detached
git checkout master

#secret key for splash_app
# the file secret_key_django_dropbox.txt is shared on dropbox in directory ubertool_keys
# needs to be placed the splash_app directory
